development:
  sessions:
    default:
      database: nostalgia-dev
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: nostalgia_production
      hosts:
        - <%= ENV['MONGOHQ_URL'] %>
      options:
        skip_version_check: true
        safe: true
